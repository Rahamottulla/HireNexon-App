import CustomLineChart from "@/features/university/components/charts/LineChart";

const PlacementTrendChart = ({ data }) => {
  return <CustomLineChart data={data} />;
};

export default PlacementTrendChart;